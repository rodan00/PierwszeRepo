package pl.camp.it.model;

public class Car extends Vehicle {
    public Car(String brand, String model, int capacity, double price, String vin) {
        super(brand, model, capacity, price, vin);
    }
}
